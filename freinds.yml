heatlTest: TEST

server:
    applicationConnectors:
        - type: http
          port: 3000

# Database settings.
database:
  # the name of the JDBC driver, mysql in our case
  driverClass: com.mysql.jdbc.Driver
  # the username
  user: admin
  # the password
  password:
  # the JDBC URL
  url: jdbc:mysql://database-2.cxdulvczjbw2.us-east-2.rds.amazonaws.com:3306/db
